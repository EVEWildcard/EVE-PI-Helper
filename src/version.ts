export const APP_VERSION = '2.4.0'

export const LATEST_CHANGE = 'v2.4.0 — “Short” rethought: PI factories are normally oversized vs. supply (they idle for free), so the chain now shows coverage % instead of crying SHORT everywhere, blames only the root product (downstream cards point at it with ⛓), suggests fixes in whole planets (“+1 planet → 133%”), and P1 output uses your planets’ real measured extractor yield from ESI instead of assuming factories run 24/7'
